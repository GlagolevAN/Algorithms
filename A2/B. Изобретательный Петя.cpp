#include <sstream>
#include <iostream>
#include <fstream>
#include <string>

using namespace std;

int main(void)
{
    int N = 0, K = 0;
    int i = 0;
    string X, Y, Z;

    getline(cin, X);
    getline(cin, Z);

    if (Z.size() < )

    Y = Z;



    while()
    {
        for (i = 0; i < )
    }

    printf("%c\n", X[0]);
    return 0;
}
